---
import Layout from '@/layouts/Layout.astro'
import Hero from '@/blocks/Hero.astro'
---

<Layout title="Page not found" isDark>
  <Hero
    headingPart1="404"
    intro="Oops, this page doesn't exist."
    backgroundMedia={null}
    buttons={[{ label: 'back to the homepage', type: 'page', url: '/' }]}
  />
</Layout>
